# Redfish exporter

This is a simple Redfish (iDRAC, iLO, XClarity) exporter for
Prometheus. The exporter uses the Redfish API to collect information
and it supports the regular /metrics endpoint to expose metrics from
the host passed via the target parameter.

https://github.com/mrlhansen/idrac_exporter
